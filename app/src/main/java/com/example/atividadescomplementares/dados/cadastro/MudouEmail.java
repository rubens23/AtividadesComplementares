package com.example.atividadescomplementares.dados.cadastro;


/**
 * callback para avisar quando o email foi alterado(usuario altera o email cadastrado)
 */
public interface MudouEmail {
    void mudouEmail(boolean mudou);
}
